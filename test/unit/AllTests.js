sap.ui.require(
    [
        // Testes unitários adicionados a partir da Fase 3 (camada de dados,
        // regras de auto-elogio e de anonimização) — lista mantida aqui.
    ],
    function () {
        QUnit.start();
    }
);
